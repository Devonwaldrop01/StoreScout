# StoreScout backend — pinned direct dependencies.
#
# Every version below was resolved together and verified to import the full app
# (`python -c "import app.main"`) and pass the test suite on Python 3.12 — the
# Railway/Dockerfile runtime (playwright base image is Ubuntu 24.04 / py3.12) —
# and also on 3.11.
# The same file drives local, Railway API, worker, and scheduler installs
# (Dockerfile: `pip install -r requirements.txt`), so all four stay identical.
#
# playwright is pinned to the exact version baked into the Dockerfile base image
# (mcr.microsoft.com/playwright/python:v1.58.0-noble) — the pre-installed
# browsers only match this release. Bump both together or scans break.
#
# To intentionally update a dependency, see docs/DEPENDENCIES.md. In short:
#   1. change the pin here, 2. rebuild the venv, 3. `python -c "import app.main"`,
#   4. run `pytest`, 5. commit the requirements.txt change on its own.

fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
curl-cffi==0.15.0
jinja2==3.1.6
playwright==1.58.0
python-multipart==0.0.32
requests==2.34.2
python-dotenv==1.2.2
stripe==15.3.0
resend==2.32.2

# SaaS additions
celery[redis]==5.6.3
redis==6.4.0
supabase==2.31.0
python-jose[cryptography]==3.5.0
anthropic==0.116.0
pydantic-settings==2.14.2
flower==2.0.1
