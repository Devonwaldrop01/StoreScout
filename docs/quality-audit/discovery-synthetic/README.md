# Synthetic discovery evidence

These files are byte-identical mirrors of `tests/evaluations/` at ff555bb. They are synthetic adversarial/wording fixtures, not the real 25-business benchmark. Use original tests from repository root if implementation work later requires regression testing. No prior tests or evaluations were rerun during this documentation handoff.
