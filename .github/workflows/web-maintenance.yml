name: Isolated Web maintenance release validation
on:
  push:
    branches: [codex/web-maintenance-entrypoint-2026-09-16]
permissions:
  contents: read
concurrency:
  group: storescout-web-maintenance-validation
  cancel-in-progress: false
jobs:
  image:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Build exact image and preserve existing regression gates
        env:
          RELEASE_SHA: ${{ github.sha }}
        run: bash scripts/validate_release_image.sh
      - name: Validate isolated Web maintenance and drain semantics
        env:
          RELEASE_SHA: ${{ github.sha }}
        run: python3 -B validation/web_maintenance/validate.py
      - name: Retain exact image and non-production evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: web-maintenance-${{ github.sha }}
          path: ${{ runner.temp }}/release-evidence/
          if-no-files-found: error
          retention-days: 14
